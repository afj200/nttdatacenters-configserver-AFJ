server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/afj200/Bootcamp-ConfigServer
          default-label: master
          search-paths:
          - '*.yaml'